<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.app.test.presentation.home.HomeFragment"
        android:label="Nearby Charging Stations"
        tools:layout="@layout/fragment_home">

        <action
            android:id="@+id/action_homeFragment_to_destinationFragment"
            app:destination="@id/destinationFragment" />
    </fragment>
    <fragment
        android:id="@+id/destinationFragment"
        android:name="com.app.test.presentation.destination.DestinationFragment"
        android:label="Station Details"
        tools:layout="@layout/fragment_destination" >
        <action
            android:id="@+id/action_destinationFragment_to_homeFragment"
            app:destination="@id/homeFragment" />
    </fragment>
</navigation>